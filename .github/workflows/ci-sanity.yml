name: Fund AI Pro CI 诊断

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sanity:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: 检查运行环境
        run: |
          echo "GitHub Actions runner 已启动"
          node --version
          npm --version
