import { createServerFn } from "@tanstack/react-start";
import { calcIndicators } from "../calc/indicators";
import { policyForFund } from "../calc/fund-type-policy";
import { estimateFundNav } from "../calc/nav-estimator";
import type { FundHistoryPoint, FundMetrics, FundQuote } from "../types";
import { fetchText, n, parseMaybeJsonp } from "./fetch-util";
import { crossCheckStockQuotes, type CrossCheckedHolding } from "./live-quote-cross-check-v2";

export type LiveHolding = { code: string; name: string; weight: number; price: number | null; pct: number | null; source: string };
type ValuationAudit = { estimateMethod?: string; estimateCoverage?: number; disclosedWeight?: number; usableWeight?: number; coverageOfDisclosed?: number; externalEstimatePct?: number | null; estimateDeviation?: number | null; estimateValidation?: string; quoteCrossCheckedWeight?: number; quoteDisagreedWeight?: number };
const VALUATION_PROVIDERS = ["https://fundcomapi.tiantianfunds.com/mm/newCore/FundValuationLast", "https://fundcomapi.eastmoney.com/mm/newCore/FundValuationLast"];
const HOLDING = "https://fundf10.eastmoney.com/FundArchivesDatas.aspx";
const NAV = "https://api.fund.eastmoney.com/f10/lsjz";
const CACHE = new Map<string, { ts: number; quote: FundQuote & ValuationAudit }>();
const TTL = 20_000;
function chinaNow() { return new Date(Date.now() + 8 * 60 * 60 * 1000); }
function today() { const d = chinaNow(); return `${d.getUTCFullYear()}-${String(d.getUTCMonth()+1).padStart(2,"0")}-${String(d.getUTCDate()).padStart(2,"0")}`; }
function htmlEntity(s: string) { return s.replace(/&nbsp;/g," ").replace(/&amp;/g,"&").replace(/&lt;/g,"<").replace(/&gt;/g,">").replace(/&#39;/g,"'").trim(); }
function stripTags(s: string) { return htmlEntity(s.replace(/<[^>]+>/g," ").replace(/\s+/g," ")); }
function parseValuationRows(text: string): Record<string, unknown>[] { const j = parseMaybeJsonp(text) as any; const rows = j?.Data || j?.data || j?.Datas || []; return Array.isArray(rows) ? rows as Record<string, unknown>[] : []; }
function pickValuation(row: Record<string, unknown> | null) { if (!row) return null; const code = String(row.FCODE ?? row.fundcode ?? row.CODE ?? "").trim(); return { code, name: String(row.SHORTNAME ?? row.name ?? "").trim(), type: String(row.FUNDTYPE ?? row.fundtype ?? row.FTYPE ?? row.type ?? "").trim(), estimate: n(row.GSZ ?? row.gsz), pct: n(row.GSZZL ?? row.gszzl), nav: n(row.NAV ?? row.dwjz), navDate: row.PDATE ?? row.jzrq ? String(row.PDATE ?? row.jzrq) : null, time: row.GZTIME ?? row.gztime ? String(row.GZTIME ?? row.gztime) : null }; }
async function fetchValuation(code: string) { const results = await Promise.all(VALUATION_PROVIDERS.map(async (base) => { try { const url = `${base}?FCODES=${encodeURIComponent(code)}&FIELDS=FCODE,SHORTNAME,FUNDTYPE,FTYPE,GSZZL,GZTIME,GSZ,NAV,PDATE&_=${Date.now()}`; const text = await fetchText(url, 8_000, { Referer: "https://fund.eastmoney.com/" }); return pickValuation(parseValuationRows(text).find((x) => String(x.FCODE ?? x.fundcode ?? x.CODE ?? "").trim() === code) ?? null); } catch { return null; } })); const valid = results.filter((x): x is NonNullable<typeof x> => !!x && x.code === code && (x.estimate != null || x.pct != null || x.nav != null)); if (!valid.length) return null; if (valid.length === 1) return { ...valid[0], validation: "单源外部估值", sourceCount: 1 }; const pctValues = valid.map((x) => x.pct).filter((x): x is number => x != null); const estimateValues = valid.map((x) => x.estimate).filter((x): x is number => x != null); const pctDeviation = pctValues.length >= 2 ? Math.abs(pctValues[0] - pctValues[1]) : null; const estimateDeviation = estimateValues.length >= 2 ? Math.abs(estimateValues[0] - estimateValues[1]) : null; const disputed = (pctDeviation != null && pctDeviation > 0.35) || (estimateDeviation != null && estimateDeviation > 0.005); const median = (values: number[]) => { const sorted = [...values].sort((a,b)=>a-b); return sorted[Math.floor(sorted.length / 2)]; }; return { ...valid[0], estimate: estimateValues.length ? median(estimateValues) : null, pct: pctValues.length ? median(pctValues) : null, validation: disputed ? "外部估值源分歧" : "双源外部估值一致", sourceCount: valid.length, sourceDeviationPct: pctDeviation, sourceDeviationNav: estimateDeviation }; }
async function fetchHistory(code: string): Promise<FundHistoryPoint[]> { try { const text = await fetchText(`${NAV}?fundCode=${code}&pageIndex=1&pageSize=300`,10_000,{Referer:"https://fund.eastmoney.com/"}); const j = parseMaybeJsonp(text) as any; return (j?.Data?.LSJZList || []).map((x:any)=>({date:String(x.FSRQ||""),nav:n(x.DWJZ)??0,changePct:n(x.JZZL)})).filter((x:FundHistoryPoint)=>x.date&&x.nav>0).reverse(); } catch { return []; } }
async function getHoldings(code: string): Promise<LiveHolding[]> { try { const raw=await fetchText(`${HOLDING}?type=jjcc&code=${code}&topline=10&year=&month=&rt=${Date.now()}`,10_000,{Referer:"https://fund.eastmoney.com/"}); const m=raw.match(/content:\\?"([\s\S]*?)\\?",arryear/i); if(!m)return[]; let html=m[1]; try{html=JSON.parse(`"${html}"`);}catch{} const out:LiveHolding[]=[]; const seen=new Set<string>(); for(const tr of html.matchAll(/<tr[^>]*>([\s\S]*?)<\/tr>/gi)){const row=tr[1]; const codeMatch=row.match(/(?:quote\.eastmoney\.com\/|href=['"][^'"]*?)(?:sz|sh)(\d{6})/i)||row.match(/(?:0|1)\.(\d{6})/); if(!codeMatch)continue; const stockCode=codeMatch[1]; if(seen.has(stockCode))continue; const anchors=[...row.matchAll(/<a[^>]*>([^<]+)<\/a>/gi)].map((x)=>stripTags(x[1])).filter(Boolean); const name=anchors.find((x)=>!/^\d{6}$/.test(x))||""; const values=[...row.matchAll(/<td[^>]*class=['"][^'"]*(?:tor|toc)[^'"]*['"][^>]*>([\s\S]*?)<\/td>/gi)].map((x)=>stripTags(x[1])).map((x)=>n(x)); const weight=values.filter((x):x is number=>x!=null&&x>0&&x<=15).at(-1); if(!weight)continue; seen.add(stockCode); out.push({code:stockCode,name,weight,price:null,pct:null,source:"东方财富基金持仓"}); if(out.length>=10)break;} return out; } catch{return[];} }
export const getCalculatedFund=createServerFn({method:"POST"}).validator((input:{code:string})=>input).handler(async({data}):Promise<FundQuote&ValuationAudit&{liveHoldings?:CrossCheckedHolding[]}>=>{const code=data.code.trim();const hit=CACHE.get(code);if(hit&&Date.now()-hit.ts<TTL)return hit.quote;try{const valuationPromise=fetchValuation(code);const historyPromise=fetchHistory(code);const valuation=await valuationPromise;const historyPoints=await historyPromise;const latest=historyPoints.at(-1);const nav=valuation?.nav??latest?.nav??null;const navDate=valuation?.navDate??latest?.date??null;const fundName=valuation?.name||code;const fundType=valuation?.type||"";const policy=policyForFund(fundType,fundName);const externalPct=valuation?.pct??(valuation?.estimate!=null&&nav!=null&&nav>0?(valuation.estimate/nav-1)*100:null);const rawHoldings=policy.allowAshareLookThrough?await getHoldings(code):[];const holdings=policy.allowAshareLookThrough?await crossCheckStockQuotes(rawHoldings):[];const lookThroughResult=policy.allowAshareLookThrough?estimateFundNav({previousNav:nav??0,positions:holdings,externalEstimatePct:externalPct}):null;const externalEstimate=valuation?.estimate!=null&&Number.isFinite(valuation.estimate)?valuation.estimate:nav!=null&&externalPct!=null&&Number.isFinite(nav)&&Number.isFinite(externalPct)?nav*(1+externalPct/100):null;const externalConfidence=valuation?.validation==="双源外部估值一致"?"medium" as const:"low" as const;const fallbackEstimate=externalEstimate!=null?{estimate:externalEstimate,pct:externalPct,confidence:externalConfidence,method:`外部实时估值 · ${valuation?.validation||"已取得实时估值"}`,validation:valuation?.validation||"外部实时估值",estimateCoverage:100,disclosedWeight:0,usableWeight:0,coverageOfDisclosed:0,crossCheckedWeight:0,disagreedWeight:0}:null;const result=lookThroughResult&&lookThroughResult.estimate!=null?lookThroughResult:fallbackEstimate;const history=historyPoints.map((x)=>x.nav);const weekBase=historyPoints[Math.max(0,historyPoints.length-6)];const monthBase=historyPoints[Math.max(0,historyPoints.length-22)];const weekPct=latest&&weekBase?.nav?(latest.nav/weekBase.nav-1)*100:null;const monthPct=latest&&monthBase?.nav?(latest.nav/monthBase.nav-1)*100:null;const metrics:FundMetrics|null=calcIndicators(history);const historyOfficialToday=latest?.date===today()&&latest.nav>0;const officialToday=historyOfficialToday;const officialNav=officialToday?latest.nav:null;const officialNavDate=officialToday?latest.date:null;const previousHistory=historyOfficialToday?historyPoints.at(-2):latest;const calculatedOfficialDayPct=officialToday&&officialNav!=null&&previousHistory?.nav?((officialNav/previousHistory.nav)-1)*100:null;const officialDayPct=calculatedOfficialDayPct??(historyOfficialToday?latest?.changePct??null:null);const dayPct=officialToday?officialDayPct:result?.pct??externalPct??null;const externalValidation=valuation?.validation||"暂无外部估值验证";const estimateValue=result?.estimate??null;const estimatePct=result?.pct??null;const estimateDeviation=externalPct!=null&&estimatePct!=null?Math.abs(externalPct-estimatePct):null;const resultCoverage=result&&"estimateCoverage" in result?result.estimateCoverage:result?.usableWeight??0;const resultDisclosed=result?.disclosedWeight??0;const resultUsable=result?.usableWeight??0;const resultCoverageOfDisclosed=result?.coverageOfDisclosed??0;const resultCrossChecked=result?.crossCheckedWeight??0;const resultDisagreed=result?.disagreedWeight??0;const resultConfidence=result?.confidence??"low" as const;const resultMethod=result?.method??policy.reason;const resultValidation=result?.validation??externalValidation;const quote:FundQuote&ValuationAudit&{liveHoldings?:CrossCheckedHolding[]}={code,name:fundName,type:fundType||policy.className,nav:officialToday?officialNav:nav,navDate:officialToday?officialNavDate:navDate,estimate:officialToday?null:estimateValue,estimatePct:officialToday?null:estimatePct,estimateTime:estimateValue!=null&&!officialToday?new Date().toISOString():null,dayPct,weekPct,monthPct,history,historyPoints,metrics,source:officialToday?"今日官方净值 · 天天基金/历史净值已确认":estimateValue!=null?`${resultMethod} · ${resultValidation} · ${externalValidation}`:`暂无可靠盘中估值 · ${externalValidation}`,officialNavPublished:officialToday,valuationStatus:officialToday?"official_nav":estimateValue!=null?"estimate":nav!=null?"waiting_official_nav":"unavailable",estimateConfidence:resultConfidence,estimateMethod:resultMethod,estimateCoverage:resultCoverage,disclosedWeight:resultDisclosed,usableWeight:resultUsable,coverageOfDisclosed:resultCoverageOfDisclosed,externalEstimatePct:externalPct,estimateDeviation,estimateValidation:`${resultValidation} · ${externalValidation}`,quoteCrossCheckedWeight:resultCrossChecked,quoteDisagreedWeight:resultDisagreed,liveHoldings:holdings,historyMae20:null,historySample20:0,historyMaxError:null,historyP95Error:null,historyMae5:null};CACHE.set(code,{ts:Date.now(),quote});return quote;}catch{return{code,name:code,type:"基金",nav:null,navDate:null,estimate:null,estimatePct:null,estimateTime:null,dayPct:null,weekPct:null,monthPct:null,history:[],historyPoints:[],metrics:null,source:"基金行情接口暂不可用",officialNavPublished:false,valuationStatus:"unavailable",estimateConfidence:"low",historyMae20:null,historySample20:0,historyMaxError:null,historyP95Error:null,historyMae5:null};}});