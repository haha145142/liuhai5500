import { createServerFn } from "@tanstack/react-start";
import { fetchText, n, parseMaybeJsonp } from "./fetch-util";
import { validateMoneyFlow } from "../calc/money-flow-validation";
import { latestTradingDate } from "./trading-day";
import type { MarketOrder } from "../types";

export type MarketMoneyFlow = MarketOrder & { marketDate:string; fetchedAt:number; turnover:number|null; previousTurnover:number|null; turnoverChangePct:number|null; turnoverState:"放量"|"缩量"|"持平"|"暂无可靠数据"; source:string; sourceCount:number; confidence:"high"|"medium"|"low"; freshness:"live"|"recent"|"stale" };
const EM_UT="fa5fd1943c7b386f172d6893dbfba10b";
const FLOW_URL=`https://push2.eastmoney.com/api/qt/clist/get?pn=1&pz=6000&po=1&np=1&fltt=2&invt=2&fid=f62&fs=${encodeURIComponent("m:0+t:6,m:0+t:80,m:1+t:2,m:1+t:23")}&fields=f62,f66,f69,f72,f75,f6&ut=${EM_UT}`;
const SNAPSHOT_URL="https://raw.githubusercontent.com/haha145142/liuhai5500/data/akshare/sector-flow.json";
const mem=new Map<string,{at:number;value:MarketMoneyFlow}>(); const TTL=90_000;
function finite(v:unknown){const x=n(v);return x!=null&&Number.isFinite(x)?x:null;} function safe(v:number|null){return v!=null&&Math.abs(v)<=1e14?v:null;} function dateLabel(d:Date){return d.toISOString().slice(0,10);} function chinaMinuteOfDay(d:Date){const t=new Intl.DateTimeFormat("en-US",{timeZone:"Asia/Shanghai",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).format(d);const[h,m]=t.split(":").map(Number);return h*60+m;}
function freshnessFor(now:Date,marketDate:string,fetchedAt:number){if(marketDate!==dateLabel(latestTradingDate(now)))return"stale"as const;const age=Math.max(0,now.getTime()-fetchedAt),min=chinaMinuteOfDay(now);if(age<=300000&&min>=570&&min<=900)return"live"as const;if(age<=1800000)return"recent"as const;return"stale"as const;}
function validateLive(flow:MarketOrder){const checked=validateMoneyFlow(flow);return{...flow,validation:checked.validation,internalDelta:checked.internalDelta,balanceDelta:checked.balanceDelta,note:checked.reason};}
function summarizeLive(rows:Record<string,unknown>[]){const keys=["f62","f66","f69","f72","f75"] as const;const sums=Object.fromEntries(keys.map(k=>[k,rows.reduce((s,r)=>s+(finite(r[k])??0),0)])) as Record<string,number>;return validateLive({main:safe(sums.f62)??0,super:safe(sums.f66)??0,large:safe(sums.f69)??0,mid:safe(sums.f72)??0,small:safe(sums.f75)??0,count:rows.length,validation:"unreliable",internalDelta:0,balanceDelta:0,note:""});}
function summarizeSnapshot(rows:Record<string,unknown>[]){const main=rows.reduce((s,r)=>s+(finite(r.main_net_inflow)??0),0);return{main,super:null,large:null,mid:null,small:null,count:rows.length,validation:"partially_consistent" as const,internalDelta:0,balanceDelta:0,note:"后台快照仅提供板块主力净流入，不推导订单分层"};}
async function fetchLiveRows(){const text=await fetchText(`${FLOW_URL}&_=${Date.now()}`,4500,{Referer:"https://data.eastmoney.com/zjlx/"});const json=parseMaybeJsonp(text) as {data?:{diff?:unknown}};const diff=json?.data?.diff;const rows=Array.isArray(diff)?diff as Record<string,unknown>[]:diff&&typeof diff==="object"?Object.values(diff) as Record<string,unknown>[]:[];if(!rows.length)throw new Error("empty-money-flow");return rows;}
async function fetchSnapshot(){const text=await fetchText(`${SNAPSHOT_URL}?_=${Date.now()}`,2500,{Accept:"application/json"});const json=JSON.parse(text) as {ok?:boolean;provider?:string;fetchedAt?:string;marketDate?:string;rows?:unknown[]};if(!json.ok||!Array.isArray(json.rows)||!json.rows.length||!json.marketDate)throw new Error("invalid-money-flow-snapshot");const rows=json.rows.filter((x):x is Record<string,unknown>=>!!x&&typeof x==="object");return{marketDate:json.marketDate,fetchedAt:Date.parse(String(json.fetchedAt??""))||Date.now(),rows,provider:String(json.provider??"AKShare")};}
async function fetchTurnoverKline(secid:string){const url=`https://push2his.eastmoney.com/api/qt/stock/kline/get?secid=${secid}&fields1=f1,f2,f3,f4,f5,f6&fields2=f51,f52,f53,f54,f55,f56,f57,f58&klt=101&fqt=1&beg=-5&end=20500101&_=${Date.now()}`;const text=await fetchText(url,4000,{Referer:"https://quote.eastmoney.com/"});const json=parseMaybeJsonp(text) as {data?:{klines?:unknown[]}};const rows=Array.isArray(json?.data?.klines)?json.data!.klines.map(x=>String(x).split(",")):[];return rows.map(parts=>({date:parts[0]||"",amount:finite(parts[5])})).filter((x):x is {date:string;amount:number}=>!!x.date&&x.amount!=null);}
export const getMarketMoneyFlow=createServerFn({method:"GET"}).handler(async():Promise<MarketMoneyFlow|null>=>{const now=new Date(),key=dateLabel(latestTradingDate(now));const hit=mem.get(key);if(hit&&Date.now()-hit.at<TTL)return hit.value;try{const snap=await fetchSnapshot(),base=summarizeSnapshot(snap.rows);if(base.count<10||!Number.isFinite(base.main))throw new Error("weak-snapshot");const result:MarketMoneyFlow={...base,marketDate:snap.marketDate,fetchedAt:snap.fetchedAt,turnover:null,previousTurnover:null,turnoverChangePct:null,turnoverState:"暂无可靠数据",source:`${snap.provider} 板块资金快照（后台采集）`,sourceCount:1,confidence:"medium",freshness:freshnessFor(now,snap.marketDate,snap.fetchedAt)};mem.set(key,{at:Date.now(),value:result});return result;}catch{}
try{const base=summarizeLive(await fetchLiveRows());if(base.count<1000||base.validation==="unreliable")return null;const[sh,sz]=await Promise.all([fetchTurnoverKline("1.000001").catch(()=>[]),fetchTurnoverKline("0.399001").catch(()=>[])]);const merged=new Map<string,number>();for(const row of[...sh,...sz])merged.set(row.date,(merged.get(row.date)??0)+row.amount);const dates=[...merged.keys()].sort();const turnover=dates.length?merged.get(dates.at(-1)!)??null:null,previousTurnover=dates.length>1?merged.get(dates.at(-2)!)??null:null;const turnoverChangePct=turnover!=null&&previousTurnover!=null&&previousTurnover>0?(turnover/previousTurnover-1)*100:null;const turnoverState=turnoverChangePct==null?"暂无可靠数据":turnoverChangePct>5?"放量":turnoverChangePct<-5?"缩量":"持平";const fetchedAt=Date.now();const result:MarketMoneyFlow={...base,marketDate:key,fetchedAt,turnover,previousTurnover,turnoverChangePct,turnoverState,source:"东方财富全A资金流 + 沪深成交额日线（单供应商；仅做内部一致性校验，不宣称双源）",sourceCount:1,confidence:"low",freshness:freshnessFor(now,key,fetchedAt)};mem.set(key,{at:fetchedAt,value:result});return result;}catch{return null;}});
