import { createServerFn } from "@tanstack/react-start";
import { fetchText, n, parseMaybeJsonp } from "./fetch-util";

type MacroOverview={bond10y:number|null;bondDate:string|null;bondChangeBp:number|null;northbound:number|null;northboundDate:string|null;csi300Pe:number|null;earningsYield:number|null;stockBondSpread:number|null;source:string};
const UT="fa5fd1943c7b386f172d6893dbfba10b";
function findDate(value:unknown):string|null{if(value==null)return null;if(Array.isArray(value)){for(const x of value){const h=findDate(x);if(h)return h;}return null;}if(typeof value!=="object")return null;const o=value as Record<string,unknown>;for(const k of ["date","trade_date","datetime","update_time","f51"]){const v=String(o[k]??"");if(/^20\d{2}[-/]\d{2}[-/]\d{2}/.test(v))return v.slice(0,10);}for(const v of Object.values(o)){const h=findDate(v);if(h)return h;}return null;}
async function bond(){try{const raw=await fetchText("https://api-ddc-wscn.awtmt.com/market/real?fields=symbol,prod_name,last_px,px_change,px_change_rate,update_time&prod_code=CN10YR.OTC",7000);const j=parseMaybeJsonp(raw) as any;const snap=j?.data?.snapshot;const row=snap?.["CN10YR.OTC"]??(Array.isArray(snap)?snap[0]:snap);const value=Array.isArray(row)?n(row[2]):n(row?.last_px);const change=Array.isArray(row)?n(row[3]):n(row?.px_change);return{value,changeBp:change==null?null:change*100,date:findDate(j)};}catch{return{value:null,changeBp:null,date:null};}}
async function northbound(){try{const raw=await fetchText(`https://push2.eastmoney.com/api/qt/kamt.kline/get?klt=101&fields1=f1,f2,f3,f4&fields2=f51,f52,f53,f54,f55,f56&lmt=10&ut=${UT}&_=${Date.now()}`,7000,{Referer:"https://data.eastmoney.com/"});const j=parseMaybeJsonp(raw) as any;const data=j?.data??{};const findSeries=(keys:string[])=>{for(const key of keys){const v=data?.[key];if(Array.isArray(v?.klines))return v.klines;if(Array.isArray(v))return v;}return[];};const sh=findSeries(["sh2hk","s2hk"]);const sz=findSeries(["sz2hk","s2sz"]);const lastDate=String((sh.at(-1)?.split?.(",")?.[0]??sz.at(-1)?.split?.(",")?.[0]??"")).slice(0,10);const last=(rows:any[])=>{const p=String(rows.at(-1)||"").split(",");return n(p.at(-1));};const a=last(sh),b=last(sz);if(a!=null||b!=null)return{value:(a??0)+(b??0),date:lastDate||findDate(j)};const any=Array.isArray(data?.klines)?data.klines:[];const p=String(any.at(-1)||"").split(",");return{value:n(p.at(-1)),date:p[0]?.slice(0,10)||findDate(j)};}catch{return{value:null,date:null};}}
async function csi300Pe(){try{const raw=await fetchText(`https://push2.eastmoney.com/api/qt/stock/get?secid=1.000300&fields=f57,f58,f162,f167&_=${Date.now()}`,6000,{Referer:"https://quote.eastmoney.com/"});const j=JSON.parse(raw) as any;const v=n(j?.data?.f162);return v==null?null:v/100;}catch{return null;}}
export const getChinaMacroOverview=createServerFn({method:"GET"}).handler(async():Promise<MacroOverview=>{const [b,north,pe]=await Promise.all([bond(),northbound(),csi300Pe()]);const earningsYield=pe!=null&&pe>0?100/pe:null;const spread=earningsYield!=null&&b.value!=null?earningsYield-b.value:null;return{bond10y:b.value,bondDate:b.date,bondChangeBp:b.changeBp,northbound:north.value,northboundDate:north.date,csi300Pe:pe,earningsYield,stockBondSpread:spread,source:"华尔街见闻 / 东方财富公开行情接口"};});
