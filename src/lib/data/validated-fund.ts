import { createServerFn } from "@tanstack/react-start";
import { getCalculatedFund } from "./live-valuation-v2";
import { getMultiSourceQuote, type MultiSourceQuote } from "./multi-source-quotes";
import { tradingDateLabel } from "./trading-day";
import type { FundQuote } from "../types";

type LiveHolding = { code:string; name:string; weight:number; price?:number|null; pct?:number|null; source?:string; quoteStatus?:string };
type AuditStats = { three:number; two:number; single:number; disputed:number; unavailable:number; usableWeight:number; weightedHealth:number; weightedAgreement:number };
const LINKED_ETF_MAP: Record<string,{etf:string;name:string}>={"008888":{etf:"159995",name:"华夏国证半导体芯片ETF"}};
function canUse(q:MultiSourceQuote){return q.pct!=null&&["three_source","two_source"].includes(q.agreement);}
function healthScore(q:MultiSourceQuote){return q.health.length?q.health.reduce((s,h)=>s+h.score,0)/q.health.length:0;}
function agreementScore(q:MultiSourceQuote){return q.agreement==="three_source"?100:q.agreement==="two_source"?85:0;}
function confidenceFrom(s:AuditStats):"high"|"medium"|"low"{if(s.usableWeight>=60&&s.weightedAgreement>=85&&s.weightedHealth>=80)return"high";if(s.usableWeight>=35&&s.weightedAgreement>=70&&s.weightedHealth>=65)return"medium";return"low";}
export const getValidatedFund=createServerFn({method:"POST"}).validator((input:{code:string})=>input).handler(async({data}):Promise<FundQuote>=>{const base=await getCalculatedFund({data:{code:data.code}});const latestTradingDate=tradingDateLabel();const isCurrentOfficial=base.nav!=null&&base.navDate===latestTradingDate&&base.officialNavPublished===true;const normalizedBase:FundQuote=isCurrentOfficial?{...base,officialNavPublished:true,valuationStatus:"official_nav",estimate:null,estimatePct:null,estimateTime:null}:{...base,officialNavPublished:false,valuationStatus:base.estimate!=null?base.valuationStatus:(base.nav!=null?"waiting_official_nav":"unavailable")};const linked=LINKED_ETF_MAP[data.code.trim()];if(linked&&normalizedBase.nav!=null&&!isCurrentOfficial){const q=await getMultiSourceQuote(linked.etf);if(q.pct!=null&&["three_source","two_source"].includes(q.agreement)){const agreement=q.agreement==="three_source"?"三源一致":"双源一致";return{...normalizedBase,estimate:normalizedBase.nav*(1+q.pct/100),estimatePct:q.pct,estimateTime:new Date().toISOString(),estimateConfidence:q.agreement==="three_source"?"high":"medium",estimateCoverage:100,valuationStatus:"estimate",source:`联接ETF实时估值 · ${linked.etf} ${linked.name} · ${agreement}`,estimateMethod:`优先使用对应ETF ${linked.etf} 盘中实时涨跌，要求双源或三源一致；不得用上一交易日涨跌冒充今日盘中数据`,estimateValidation:`对应ETF ${linked.etf} · ${agreement} · 偏差 ${q.deviationPct==null?"—":q.deviationPct.toFixed(2)+"%"}`};}}
  const holdings:LiveHolding[]=((normalizedBase as FundQuote & {liveHoldings?:LiveHolding[]}).liveHoldings||[]);
  if(isCurrentOfficial||!holdings.length||normalizedBase.nav==null)return isCurrentOfficial?normalizedBase:{...normalizedBase,estimate:null,estimatePct:null,estimateTime:null,valuationStatus:normalizedBase.nav!=null?"waiting_official_nav":"unavailable",estimateConfidence:"low",source:"当前没有经过双源/三源验证的盘中估值 · 不显示猜测数字",estimateValidation:"实时估值必须经过交叉验证"};
  const validated=await Promise.all(holdings.map(async holding=>({holding,quote:await getMultiSourceQuote(holding.code)})));
  const usable=validated.filter(x=>x.holding.weight>0&&canUse(x.quote));
  if(!usable.length)return{...normalizedBase,estimate:null,estimatePct:null,estimateTime:null,estimateConfidence:"low",valuationStatus:normalizedBase.nav!=null?"waiting_official_nav":"unavailable",source:"当前重仓行情没有双源/三源共识 · 不显示模拟估值",estimateValidation:"当前未获得双源/三源实时共识"};
  const pct=usable.reduce((s,x)=>s+x.holding.weight*(x.quote.pct as number),0)/100;const estimate=normalizedBase.nav*(1+pct/100);const stats:AuditStats={three:usable.filter(x=>x.quote.agreement==="three_source").length,two:usable.filter(x=>x.quote.agreement==="two_source").length,single:validated.filter(x=>x.quote.agreement==="single_source").length,disputed:validated.filter(x=>x.quote.agreement==="disputed").length,unavailable:validated.filter(x=>x.quote.agreement==="unavailable").length,usableWeight:usable.reduce((s,x)=>s+x.holding.weight,0),weightedHealth:usable.reduce((s,x)=>s+x.holding.weight*healthScore(x.quote),0)/Math.max(.0001,usable.reduce((s,x)=>s+x.holding.weight,0)),weightedAgreement:usable.reduce((s,x)=>s+x.holding.weight*agreementScore(x.quote),0)/Math.max(.0001,usable.reduce((s,x)=>s+x.holding.weight,0))};const coverage=Math.min(100,stats.usableWeight);const health=Math.round(stats.weightedHealth);const agreement=Math.round(stats.weightedAgreement);const confidence=confidenceFrom(stats);return{...normalizedBase,estimate,estimatePct:pct,estimateConfidence:confidence,estimateCoverage:coverage,source:`自有穿透估值 · ${stats.three>0?"三源/双源":"双源"}实时校验 · 健康${health}`,estimateValidation:`三源一致 ${stats.three} · 双源一致 ${stats.two} · 单源拒绝 ${stats.single} · 分歧 ${stats.disputed} · 覆盖 ${coverage.toFixed(1)}% · 一致度 ${agreement}`,liveHoldings:holdings.map(h=>{const hit=validated.find(x=>x.holding.code===h.code);if(!hit)return h;const q=hit.quote;const isUsable=q.pct!=null&&["three_source","two_source"].includes(q.agreement);const label=q.agreement==="three_source"?"三源一致":q.agreement==="two_source"?"双源一致":q.agreement==="disputed"?"来源分歧":q.agreement==="single_source"?"单源拒绝":"暂无行情";return{...h,price:isUsable?q.price:null,pct:isUsable?q.pct:null,source:label};})}as FundQuote;});
