import { sharedCacheConfigured, sharedCacheGet, sharedCacheSet } from "./shared-cache.ts";
export type CircuitState = "CLOSED" | "OPEN" | "HALF_OPEN";
export type ProviderHealth = { provider: string; endpoint: string; state: CircuitState; consecutiveFailures: number; lastSuccess: number; lastFailure: number; latencyMs: number; successCount: number; requestCount: number; successRate: number };
type State = ProviderHealth & { openedAt: number };
const FAILURE_THRESHOLD = 5;
const OPEN_MS = 30_000;
const SHARED_HEALTH_TTL_MS = 60_000;
const SHARED_HEALTH_READ_MS = 10_000;
const states = new Map<string, State>();
const sharedReadAt = new Map<string, number>();
function key(provider: string, endpoint: string) { try { return `${provider}:${new URL(endpoint).host}`; } catch { return `${provider}:${endpoint}`; } }
function sharedKey(provider: string, endpoint: string) { return `fund-ai-pro:provider-health:${key(provider, endpoint)}`; }
export function providerFromUrl(endpoint: string) { try { const host = new URL(endpoint).hostname.toLowerCase(); if (host.includes("eastmoney")) return "东方财富"; if (host.includes("gtimg")) return "腾讯财经"; if (host.includes("sina")) return "新浪财经"; if (host.includes("githubusercontent")) return "AKShare快照"; if (host.includes("10jqka")) return "同花顺"; if (host.includes("cls.cn")) return "财联社"; if (host.includes("jin10")) return "金十数据"; if (host.includes("wscn")) return "华尔街见闻"; return host; } catch { return "unknown"; } }
function localState(provider: string, endpoint: string): State { const k = key(provider, endpoint); const current = states.get(k); if (current) { if (current.state === "OPEN" && Date.now() - current.openedAt >= OPEN_MS) current.state = "HALF_OPEN"; return current; } const empty: State = { provider, endpoint, state: "CLOSED", consecutiveFailures: 0, lastSuccess: 0, lastFailure: 0, latencyMs: 0, successCount: 0, requestCount: 0, successRate: 1, openedAt: 0 }; states.set(k, empty); return empty; }
export function getProviderHealth(provider: string, endpoint: string): ProviderHealth { return localState(provider, endpoint); }
export function providerAllowed(provider: string, endpoint: string) { const state: CircuitState = localState(provider, endpoint).state; return state !== "OPEN"; }
export async function providerAllowedAsync(provider: string, endpoint: string) { const local = localState(provider, endpoint); const now = Date.now(); if (local.state === "OPEN" && now - local.openedAt >= OPEN_MS) local.state = "HALF_OPEN"; let state: CircuitState = local.state; if (state === "OPEN" || !sharedCacheConfigured()) return state !== "OPEN"; const k = key(provider, endpoint); const lastRead = sharedReadAt.get(k) ?? 0; if (now - lastRead < SHARED_HEALTH_READ_MS) { state = local.state; return state !== "OPEN"; } sharedReadAt.set(k, now); const shared = await sharedCacheGet<State>(sharedKey(provider, endpoint)); if (!shared?.value) { state = local.state; return state !== "OPEN"; } const merged = shared.value; if (merged.state === "OPEN") { if (now - merged.openedAt >= OPEN_MS) local.state = "HALF_OPEN"; else { local.state = "OPEN"; local.consecutiveFailures = Math.max(local.consecutiveFailures, merged.consecutiveFailures); local.openedAt = merged.openedAt; } } else if (merged.state === "HALF_OPEN") { local.state = "HALF_OPEN"; local.consecutiveFailures = Math.max(local.consecutiveFailures, merged.consecutiveFailures); local.openedAt = merged.openedAt; } state = local.state; return state !== "OPEN"; }
export function recordProviderSuccess(provider: string, endpoint: string, latencyMs: number) { const current = localState(provider, endpoint); current.requestCount += 1; current.successCount += 1; current.successRate = current.successCount / Math.max(1, current.requestCount); current.consecutiveFailures = 0; current.lastSuccess = Date.now(); current.latencyMs = Math.round(latencyMs); current.state = "CLOSED"; current.openedAt = 0; }
export function recordProviderFailure(provider: string, endpoint: string, latencyMs: number) { const current = localState(provider, endpoint); current.requestCount += 1; current.consecutiveFailures += 1; current.successRate = current.successCount / Math.max(1, current.requestCount); current.lastFailure = Date.now(); current.latencyMs = Math.round(latencyMs); if (current.state === "HALF_OPEN" || current.consecutiveFailures >= FAILURE_THRESHOLD) { current.state = "OPEN"; current.openedAt = Date.now(); } }
export async function recordProviderSuccessAsync(provider: string, endpoint: string, latencyMs: number) { recordProviderSuccess(provider, endpoint, latencyMs); if (sharedCacheConfigured()) await sharedCacheSet(sharedKey(provider, endpoint), localState(provider, endpoint), SHARED_HEALTH_TTL_MS); }
export async function recordProviderFailureAsync(provider: string, endpoint: string, latencyMs: number) { recordProviderFailure(provider, endpoint, latencyMs); if (sharedCacheConfigured()) await sharedCacheSet(sharedKey(provider, endpoint), localState(provider, endpoint), SHARED_HEALTH_TTL_MS); }
export function listProviderHealth(): ProviderHealth[] { return [...states.values()].map(({ openedAt: _openedAt, ...entry }) => ({ ...entry })); }