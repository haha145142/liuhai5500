export type RiskStats = { returnPct:number|null;volatilityPct:number|null;maxDrawdownPct:number|null;sharpe:number|null;sample:number};
const clean=(v:number[])=>v.filter(x=>Number.isFinite(x)&&x>0);export const periodReturn=(v:number[])=>{const a=clean(v);return a.length<2?null:(a.at(-1)!/a[0]-1)*100};export const maxDrawdown=(v:number[])=>{const a=clean(v);if(a.length<2)return null;let p=a[0],m=0;for(const x of a){p=Math.max(p,x);m=Math.max(m,(p-x)/p*100)}return m};export const dailyReturns=(v:number[])=>{const a=clean(v),o:number[]=[];for(let i=1;i<a.length;i++)o.push(a[i]/a[i-1]-1);return o};export const volatilityAnnualized=(v:number[])=>{const r=dailyReturns(v);if(r.length<2)return null;const m=r.reduce((a,b)=>a+b,0)/r.length,s=Math.sqrt(r.reduce((a,x)=>a+(x-m)**2,0)/(r.length-1));return s*Math.sqrt(252)*100};export const sharpeAnnualized=(v:number[],rfAnnualPct=1.5)=>{const r=dailyReturns(v);if(r.length<2)return null;const m=r.reduce((a,b)=>a+b,0)/r.length,s=Math.sqrt(r.reduce((a,x)=>a+(x-m)**2,0)/(r.length-1));if(!s)return null;return(m-(Math.pow(1+rfAnnualPct/100,1/252)-1))/s*Math.sqrt(252)};export const riskStats=(v:number[]):RiskStats=>({returnPct:periodReturn(v),volatilityPct:volatilityAnnualized(v),maxDrawdownPct:maxDrawdown(v),sharpe:sharpeAnnualized(v),sample:clean(v).length});export const normalized=(v:number[])=>{const a=clean(v);return a.length?a.map(x=>x/a[0]):[]};
