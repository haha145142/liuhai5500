import { useEffect, useMemo, useState } from "react";
import { Glass, SectionTitle } from "@/components/ui/Glass";
import { useApp } from "@/lib/store";
import { alertKindLabel, evaluateFundAlerts, loadAlertEvents, loadAlertRules, notifyAlertEvents, saveAlertRules, DEFAULT_ALERT_RULES, type AlertKind, type AlertRule } from "@/lib/alerts";
const ALERT_CHECK_MS=180_000;type NotificationState=NotificationPermission|"unsupported";
export function SmartAlerts(){const funds=useApp(s=>s.funds),portfolio=useApp(s=>s.portfolio),snapshot=useApp(s=>s.snapshot);const[rules,setRules]=useState<AlertRule[]>(()=>loadAlertRules());const[events,setEvents]=useState(()=>loadAlertEvents());const[code,setCode]=useState("");const[kind,setKind]=useState<AlertKind>("fund_up");const[threshold,setThreshold]=useState("3");const[notificationState,setNotificationState]=useState<NotificationState>("unsupported");const fundsCodes=useMemo(()=>[...new Set(portfolio.map(h=>h.code))],[portfolio]);const sectorCodes=useMemo(()=>[...new Map((snapshot?.sectors||[]).map(s=>[s.bkCode,s.name])).entries()],[snapshot?.sectors]);const isSectorRule=kind==="sector_change",kindNeedsFund=!isSectorRule;
 const refreshAlerts=()=>{const created=evaluateFundAlerts(funds,snapshot?.sectors||[]);if(created.length)notifyAlertEvents(created);setEvents(loadAlertEvents());};
 useEffect(()=>{if(typeof Notification==="undefined"){setNotificationState("unsupported");return;}setNotificationState(Notification.permission);},[]);useEffect(()=>{const timer=window.setInterval(refreshAlerts,ALERT_CHECK_MS);return()=>window.clearInterval(timer);},[funds,snapshot?.sectors]);
 const enable=async()=>{if(typeof Notification==="undefined"){setNotificationState("unsupported");return;}try{setNotificationState(await Notification.requestPermission());}catch{setNotificationState(Notification.permission);}};
 const add=()=>{const value=Number(threshold),validTarget=kindNeedsFund?fundsCodes.includes(code):sectorCodes.some(([x])=>`sector:${x}`===code);if(!Number.isFinite(value)||value<=0||!validTarget)return;const rule:AlertRule={id:`${code}-${kind}-${value}-${Date.now()}`,code,kind,threshold:value,enabled:true};const next=[rule,...rules].slice(0,50);setRules(next);saveAlertRules(next);refreshAlerts();};
 const presets=()=>{const existing=new Set(rules.map(r=>r.id));const next=[...rules,...DEFAULT_ALERT_RULES(fundsCodes).filter(r=>!existing.has(r.id))].slice(0,50);setRules(next);saveAlertRules(next);};
 const toggle=(id:string)=>{const next=rules.map(r=>r.id===id?{...r,enabled:!r.enabled}:r);setRules(next);saveAlertRules(next);};const remove=(id:string)=>{const next=rules.filter(r=>r.id!==id);setRules(next);saveAlertRules(next);};
 return <Glass><SectionTitle title="智能提醒" hint="3分钟检查 · 1小时冷却"/><div className="rounded-2xl bg-bg-elevated px-3 py-3"><div className="grid grid-cols-3 gap-1.5"><select value={code} onChange={e=>setCode(e.target.value)} aria-label="提醒对象" className="h-10 rounded-xl bg-white/70 px-2 text-[10px] outline-none ring-1 ring-border"><option value="">选择{isSectorRule?"板块":"基金"}</option>{kindNeedsFund?fundsCodes.map(x=><option key={x} value={x}>{x}</option>):sectorCodes.map(([x,name])=><option key={x} value={`sector:${x}`}>{name}</option>)}</select><select value={kind} onChange={e=>{setKind(e.target.value as AlertKind);setCode("");}} aria-label="提醒类型" className="h-10 rounded-xl bg-white/70 px-2 text-[10px] outline-none ring-1 ring-border"><option value="fund_up">涨幅 ≥</option><option value="fund_down">跌幅 ≤</option><option value="drawdown">回撤 ≥</option><option value="valuation_range">估值区间 ±</option><option value="sector_change">板块变化 ±</option><option value="three_down">连续3日下跌</option></select><input value={threshold} onChange={e=>setThreshold(e.target.value)} inputMode="decimal" aria-label="提醒阈值" className="h-10 rounded-xl bg-white/70 px-2 text-[10px] outline-none ring-1 ring-border" placeholder={kind==="three_down"?"固定3个交易日":"阈值 %"}/></div><button type="button" onClick={add} disabled={kindNeedsFund?!fundsCodes.includes(code):!code.startsWith("sector:")} className="mt-2 h-10 w-full rounded-xl bg-accent text-xs font-semibold text-accent-fg disabled:opacity-50">添加提醒规则</button><button type="button" onClick={presets} disabled={!fundsCodes.length} className="mt-1.5 h-9 w-full rounded-xl bg-white/75 text-[10px] font-semibold ring-1 ring-white/90 disabled:opacity-50">一键开启持仓默认提醒（涨跌3% + 连续3日下跌）</button><div className="mt-1.5 grid grid-cols-2 gap-1.5"><button type="button" onClick={()=>void enable()} disabled={notificationState==="granted"||notificationState==="unsupported"} className="h-9 rounded-xl bg-white/75 text-[10px] font-semibold ring-1 ring-white/90 disabled:opacity-50">{notificationState==="granted"?"系统提醒已开启":notificationState==="denied"?"系统提醒已拒绝":notificationState==="unsupported"?"当前浏览器不支持":"开启系统提醒"}</button><button type="button" onClick={refreshAlerts} className="h-9 rounded-xl bg-white/75 text-[10px] font-semibold ring-1 ring-white/90">立即检查</button></div></div><div className="mt-2 space-y-1.5">{rules.length?rules.map(r=><div key={r.id} className="flex items-center gap-2 rounded-xl bg-white/55 px-2.5 py-2 text-[9px]"><span className={`size-2 rounded-full ${r.enabled?"bg-emerald-500":"bg-slate-300"}`}/><span className="min-w-0 flex-1 truncate">{r.code.replace(/^sector:/,"")} · {alertKindLabel[r.kind]} {r.kind==="three_down"?"":`${r.threshold.toFixed(1)}%`}</span><button type="button" onClick={()=>toggle(r.id)} className="rounded-lg bg-white/75 px-2 py-1">{r.enabled?"停用":"启用"}</button><button type="button" onClick={()=>remove(r.id)} className="rounded-lg bg-white/75 px-2 py-1">删除</button></div>):<div className="rounded-xl bg-white/45 px-3 py-3 text-[9px] text-muted">还没有提醒规则。</div>}</div>{events.length?<div className="mt-2 rounded-xl bg-amber-50/65 px-2.5 py-2"><div className="text-[9px] font-semibold">最近触发</div>{events.slice(0,5).map(e=><div key={e.id} className="mt-1 text-[8px] leading-[1.4] text-muted">{new Date(e.triggeredAt).toLocaleTimeString("zh-CN",{hour:"2-digit",minute:"2-digit",timeZone:"Asia/Shanghai"})} · {e.message}</div>)}</div>:null}<p className="mt-2 text-[8px] leading-relaxed text-subtle">连续3日下跌基于最近3个可靠官方净值日。系统通知依赖浏览器权限；当前版本页面保持打开时可检查，真正离线后台推送仍需额外 Push/VAPID 服务。</p></Glass>;
}
