import { Link } from "@tanstack/react-router";
import { Glass } from "@/components/ui/Glass";
import { fmtPctShort } from "@/lib/format";
type Row={id:string;name:string;bkCode?:string;change:number|null};
export function MarketHeatmap({ boards, sectors }: { boards: Row[]; sectors: Row[] }) { const source=boards.length?boards:sectors;const rows=source.filter(x=>x.change!=null).sort((a,b)=>(b.change??0)-(a.change??0)).slice(0,24);return <Glass className="mt-3 rounded-[22px] p-3"><div className="flex items-center justify-between"><div><div className="text-[15px] font-semibold">行业 / 主题涨跌热力图</div><div className="mt-0.5 text-[9px] text-muted">按最近可用行情排列；点击进入轮动雷达与相关基金池</div></div><span className="text-[8px] text-subtle">{rows.length} 项</span></div>{rows.length?<div className="mt-2 grid grid-cols-3 gap-1.5">{rows.map(r=><Link key={`${r.id}-${r.bkCode||r.name}`} to="/rotation" onClick={()=>{try{localStorage.setItem("fund_ai_pro_rotation_focus",r.id);}catch{}}} className="rounded-[14px] bg-white/62 p-2 ring-1 ring-white/75 active:scale-[.99]"><div className="truncate text-[9px] font-medium">{r.name}</div><div className={`mt-1 text-[12px] font-bold tabular-nums ${(r.change??0)>=0?"text-red-500":"text-emerald-600"}`}>{fmtPctShort(r.change)}</div></Link>)}</div>:<div className="mt-2 rounded-xl bg-white/55 px-3 py-4 text-center text-[9px] text-muted">等待板块行情</div>}<div className="mt-1.5 text-[7px] text-subtle">点击后进入 AI 轮动雷达；行情与新闻分开验证。</div></Glass>; }
