# Fund AI Pro staged data design

## Product rule
- Never block the whole screen on unrelated data sources.
- Reuse last reliable local cache immediately.
- Each module refreshes independently when opened.
- New positions trigger an immediate per-fund refresh; no global refresh lock.
- Background refresh may update cards in place without replacing valid data with empty states.
- Show explicit stale/recent metadata rather than blank loading placeholders.
